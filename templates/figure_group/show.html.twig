{% extends 'base.html.twig' %}

{% block title %}FigureGroup{% endblock %}

{% block body %}
    <h1>FigureGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ figure_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ figure_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_figure_group_index') }}">back to list</a>

    <a href="{{ path('app_figure_group_edit', {'id': figure_group.id}) }}">edit</a>

    {{ include('figure_group/_delete_form.html.twig') }}
{% endblock %}
