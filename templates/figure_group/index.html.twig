{% extends 'base.html.twig' %}

{% block title %}FigureGroup index{% endblock %}

{% block body %}
    <h1>FigureGroup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for figure_group in figure_groups %}
            <tr>
                <td>{{ figure_group.id }}</td>
                <td>{{ figure_group.name }}</td>
                <td>
                    <a href="{{ path('app_figure_group_show', {'id': figure_group.id}) }}">show</a>
                    <a href="{{ path('app_figure_group_edit', {'id': figure_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_figure_group_new') }}">Create new</a>
{% endblock %}
