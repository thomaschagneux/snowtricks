<header class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <!-- Logo à gauche -->
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('assets/img/logo.png') }}" alt="Logo" width="50" height="50">
        </a>

        <!-- Bouton mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation -->
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item border-end pe-3">
                    <a class="nav-link text-primary text-decoration-underline" href="{{ path('app_home') }}">Home</a>
                </li>
                <li class="nav-item border-end pe-3">
                    <a class="nav-link text-primary text-decoration-underline" href="{{ path('app_register') }}">S'enregistrer</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-primary text-decoration-underline" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            </ul>
        </div>
    </div>
</header>